export interface Icocktail {
  id: string,
  name: string,
  img: string,
  glass: string,
  ingredients: string[],
  numIngredients: number,
  instructions: string,
  measures: string[],
  numMeasures: number
}
